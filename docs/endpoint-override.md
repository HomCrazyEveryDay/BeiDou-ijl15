# 客户端连接线路

启动器 2.0.7 起，连接由 HTTPS 线路接口下发，后台的“游戏管理 → 启动器线路”编辑。玩家只看到线路名，不再读取 config.ini 或显示地址。DLL 验证授权启动器传入的 IPv4 和端口；服务端按公开／测试资格下发目录，新增线路无需再发布 DLL。当前默认目录与旧配置迁移规则见服务端文档。

完整接口、部署、验证和回滚流程见服务端 docs/launcher-routes.md。客户端实际连接地址仍可通过网络工具观察，隐藏界面不等于网络层隐藏。
