# 火焰喷射旁观崩溃（2026-09-22）

## 首次异常证据

抽象派角色46，PID7496/TID16796，run `f8456ad07da2b97384dbe79373a7af9c`，连接 `7abaebba9a3443b98e27e5ab305a7a41-7779`。
13:04:33.119（北京时间）收到魔法攻击0xBC，size39；13:04:33.148 首次访问违规，13:04:35.069 未处理异常，服务端13:04:35.968收到Connection reset。
客户端已有条件及最终dump，首次异常上下文尚未被退出过程覆盖。

- `RESMAN.DLL+38C0` / `C0000005`，读取地址0，EAX/ESI为0。
- 调用链：`403AE7 -> 436640 -> 982C9D -> 980A1B`，为旁观魔法处理内即时资源请求，区别于此前延迟弹道 `/1`。
- 原生魔法处理帧EBP `0019E844`，`[EBP-14] = 22151001`，`[EBP+0C] = 18ABC7EC`（技能对象），技能对象首DWORD也为22151001。
- `[18ABC7EC+9C] = 0`。原生 `411256` 返回这个特效路径；`982C8B` 将空值放入EAX，`982C97` 将其作为实参，`982C98` 调用4365DB，资源管理器最终空指针异常。4365DB帧`0019E748`的第一实参亦为0。
- dump记录的ijl15 PE timestamp `6AB209F0` 与复测时本地安装DLL一致，证明不是沿用前一版DLL；不将timestamp单独表述为整文件哈希验证。

## 原版差异和修复

083 `982C0C..982C1A` 将冰点寒气22121000和火焰喷射22151001送入 `982C20` 旧蓄力范围屏幕特效路径。084 `9C2E52..9C3109` 的对应屏幕特效分派已经移除这两个技能；普通法师技能仍有对应路径。

在已有原子安装表加入 `982C0C` 的完整原指令校验，并跳转到原生默认后续处理 `982D98`。此处之前的普通法师分派保留。技能的龙动作、逐目标伤害和正常命中特效在此前原生流程中处理，不以补假路径或吞资源异常替代真实兼容。

三层评估：仅客户端旧硬编码分派受影响；服务端伤害公式、XML及IMG无需变更。不存在资源数据错误的证据，不新增假资源或改原版动作。

## 验证和交付边界

`EvanBlazeDispatchTest.ps1` 在隔离进程中映射真实083代码，先执行原分派证明两个技能进入旧路径，再安装补丁证明进入默认处理；同时验证2111002、2121001、2221001、2321001、2121007、2221007、2321008及12111003分支不变，签名损坏时整体拒绝安装。既有旁观弹道、黑暗迷雾范围分支、暴击位图及动作缓存测试一起执行。

DLL必须Release|x86构建，确认游戏关闭后同步并核对SHA-256。以上为原生指令隔离测试，不能代替用户双开测试：旁观保持不施法，复测火焰喷射及冰点寒气，并覆盖其他已修复技能。构建通过不报告成实机已验证。
