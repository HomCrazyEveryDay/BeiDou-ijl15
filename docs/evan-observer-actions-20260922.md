# 龙神动作与旁观显示检查（2026-09-22）

## 本轮确定修复

- 火焰环 22101000 的 `fireCircle`、地震 22161001 的 `Earthquake` 未注册于 083 动作表，Character/00002000.img 也缺节点。追加人物动作 163、164 和龙动作 22、23，不替换已有技能动作。扩展加载、查询、销毁边界和独立动画缓存。
- 从已核实 GMS084 `exports/evan/gms084/Data` 增量合并原版 7/9 帧节点；服务端 Character XML 同步。IMG 141421 → 142421 字节，无关像素和保存后反读通过。清单为 `exports/evan/area-actions-staged/merge-verified.json`。
- 鬼刻符 22161002：083 MobStat +1D0/+1D4 已解码状态和技能，CMob 内嵌 MobStat 位于 +1A0，因此对应 CMob +370/+374。083 00665CFD 的特效状态表漏此项；084 0067BFE1..0067BFFA 登记该状态。006660BD 跳板移植相同登记，复用原生刷新/销毁，不修改标记贴图、数值或期限。
- 服务端部分补发数字及削弱免疫路径用 `raw + INT_MAX` 解码，与已有符号位编码相差 1。统一为 `raw & INT_MAX`，削弱后也复用同一编码方法。普通路径和暴击路径按相同正数计算。

## 尚待实测定位

魔法导弹 22001001 的 magicmissile 人物动作、龙动作及 hit 已存在；原版没有独立 ball 节点，不能凭缺少 ball 判定资源损坏。上一轮误将龙推力 22141001 当成魔翼痛击，现纠正：魔翼痛击是 22151002，动作 killingWing，原版为单目标锁定；服务端只向施法者发送 HOMING_BEACON，并保存一个目标 OID。另见 evan-killer-wings.md。现有双开日志没有采到这两项，尚不能确定实际缺失发生于施法动作、命中特效还是伤害呈现。

诊断改为每技能最多 8 次、最多 32 种技能；客户端总计最多 8192 条记录。新增 remote_action（动作索引/原生是否接受）、remote_geometry（目标索引/原生矩形和投射物中止条件）、发送 footer（暴击掩码）日志。范围日志的 projectileWouldBreak 是普通投射物条件，不代表获豁免的范围技能实际中止。继续保留服务端解析、广播、实际伤害路径日志。客户端 `BEIDOU_EVAN_ATTACK_LOG=0`、服务端 `-Dbeidou.evanAttackLog=false` 可关闭。

## 验证与边界

隔离执行原始 083 指令验证补丁签名、新增缓存互不覆盖、回收、鬼刻符无状态/施加/刷新/清除、范围分派和暴击 footer。服务端回归覆盖八目标暴击标志、状态封包以及暴击编码/削弱免疫正数一致性。Release|x86 构建并同步本地 DLL，资源反读通过。自动验证不等于双开显示已经确认。

用户复测只需两端重新启动：分别释放魔法导弹、火焰环、地震、魔翼痛击、鬼刻符各 2–3 次，期间旁观端不施法；检查自身/旁观动画、Boss 伤害数字、暴击颜色，以及鬼刻符标记刷新与到期消失。保留日志，反馈哪一项仍异常即可。

## 闪电箭补充

22111000 原版动作名为 lightingBolt（保留原拼写）。人物基础节点和 083 动作注册缺失，龙动作已存在。新增人物动作 165、龙动作 24，复用独立缓存和原生加载/销毁机制。由已验证 GMS084 增量补入 8 帧人物节点，同步服务端 XML；IMG 142421 → 142838 字节，无关像素与反读一致。未修改技能伤害、文本或龙贴图。构建/隔离测试不能代替用户实机验证。
