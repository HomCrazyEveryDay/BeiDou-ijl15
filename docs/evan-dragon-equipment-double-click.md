# 龙装备双击卸下（2026-09-23）

用户实测四件可双击穿戴，但龙装备栏双击不能卸下。

083 `CDraggableItem` 双击入口 `004EFD25` 已由龙装备栏的 `007FD869` 创建的原生拖拽对象调用。其卸装分派 `004F0BE4..004F0CF9` 只接受普通装备栏 RTTI `00BF0E48` 和宠物装备栏 `00BF0E4C`，缺少龙装备栏 `00BF0E50`。龙装备栏鼠标接口 vtable `00B38B40`，其 IsKindOf 为 `007FF9EC`，确实使用 `00BF0E50`。

在 `004F0CE2` 的最后一次窗口类型调用／结果检查处追加兼容：先保留原类型检查；失败时，仅当装备类型为 1、槽位为 -1000～-1003，检查原生龙装备窗口类型。通过后继续原生 `004F0CFF`，最终调用 `004F0E97` 卸装函数。保留背包空位、动作间隔、地图限制、物品存在性及 `00A0900A` 原生移动请求，不伪造客户端卸装状态，不改服务端校验、资源和物品 ID。

补丁纳入 EvanRuntime 的整组签名检查及原子安装。`EvanBlazeDispatchTest.ps1` 在隔离进程加载真实 083 指令，证明原路径拒绝龙装备；安装后检查四槽可达、边界外槽位／非装备类型／其他窗口拒绝、宠物装备路径保持原结果，同时验证实际 trampoline 调用栈及签名损坏时拒绝整组安装。该测试不启动游戏或连接服务端。

交付使用 Release|x86 构建，客户端关闭后同步到 BeiDou-Client/ijl15.dll 并核对 SHA-256。用户实测四槽双击卸下、背包满时不丢物品、普通／现金／宠物装备卸下不受影响；实机结果待反馈。
