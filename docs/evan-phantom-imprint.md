# 鬼刻符（22161002）

## 原生机制与修改范围

本地 083 BeiDou.exe 的 MobStat 位 34（BEF890）解码于 0078BA0D：short 数值写入 +1D0，技能 ID 写入 +1D4。00791F58 的魔法伤害计算读取 +1D0，以 `(100 + x) / 100` 乘伤害；不检查施法者身份。因此自身和其他角色的魔法攻击共享目标减益，物理伤害不扩展。服务端不得再乘一次。重复封包覆盖该字段，不累计。

按用户要求将最高增幅从 5% 提到 10%，保留原等级阶梯：1–4 级 2%、5–8 级 4%、9–12 级 6%、13–16 级 8%、17–20 级 10%。原值依次为 1/2/3/4/5。持续时间仍为 5 秒，其余技能数值和图片保持原版。服务端 XML、中文 String、客户端 IMG 同步。

服务端允许该技能的单一 PHANTOM_IMPRINT 状态通过 Boss 通用状态免疫检查，保留元素抗性检查。使用原有按状态键覆盖的机制，旧状态清空并取消旧计时器；过期回调仅能移除自身仍占有的状态。交替施法者连续标记 20 次及过期回调测试验证不叠加、不提前移除新标记，最后正常过期。状态施加及控制封包测试验证位 34、数值和技能 ID。

## 动作兼容与资源来源

来源为已验证的 GMS084 `exports/evan/gms084/Data`。Character/00002000.img 源 SHA-256：`A175EE36D44972777A71C51F61D047F63B9EFF976B3CA81263D65AECA565E5E1`。

技能 hit/mob 与 Dragon/2216.img 的 ghostLettering 已存在；缺失 Character/ghostLettering，增量合并原版完整节点并同步服务端 XML。客户端 Character IMG 从 140320 增至 140833 字节，无关节点和像素不变，保存后反读通过。

083 StringPool 5502 的未使用 dragonFury 动作槽（人物注册 004A5EE3、龙注册 004A9C5D）改为注册原始 ghostLettering 名称。保留原版人物/龙动作，不以其他技能动画替代。5508 的 dragonFury 被动效果路径不变。

Skill/2216.img 仅改 20 个 x 字段，文件大小不变，完整像素签名比较无其他变化；String/Skill.img 仅改该技能描述和 20 条等级说明，增加 940 字节。orange-wz verify 41 项匹配，MapleLib 交叉反读一致。

## 实机验证

由用户测试：重新启动客户端，在可承受多次攻击的 Boss 上施放，检查人物/龙动作及目标标记；同等级重复标记仅刷新时间。比较未标记、标记中、过期后自己和队友的魔法伤害，注意伤害浮动与上限。物理攻击不获得此魔法增幅。本记录的自动测试和资源核对不代表已完成客户端实测。
