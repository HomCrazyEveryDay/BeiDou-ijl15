# BeiDou ijl15 Rules

## Commit Message Rules

- 提交信息必须使用中文。
- 提交标题必须以 Conventional Commit 类型开头，例如 `fix:`、`feat:`、`docs:`、`chore:`、`refactor:`、`test:`、`build:`、`ci:`、`perf:` 或 `style:`。
- 提交正文必须用 `-` 列出主要改动，每条一行。
- 不要提交本地环境文件、日志文件或与当前任务无关的脏文件。

示例：

```text
fix: 汉化好友列表提示

- 通过 ijl15.dll 运行时替换客户端硬编码文本
- 保持 BeiDou.exe 不变
```
